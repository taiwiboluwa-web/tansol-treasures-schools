import { requireRole } from '@/lib/auth/server'; export default async function ParentPage(){const s=await requireRole(['parent','admin']);return <main className="page-shell py-20"><p className="eyebrow text-[var(--gold)]">Parent portal</p><h1 className="display mt-4 text-6xl md:text-8xl">Stay close to the learning.</h1><p className="mt-6 max-w-2xl leading-7 text-[var(--muted)]">Welcome {s.fullName}. This protected hub will bring together linked learners, attendance, notices and academic progress.</p><div className="mt-12 grid gap-4 md:grid-cols-3">{['Linked learners','Attendance','School notices'].map((x,i)=><div key={x} className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7"><span className="font-mono text-xs text-[var(--gold)]">0{i+1}</span><h2 className="display mt-12 text-3xl">{x}</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Your parent dashboard will surface this information here.</p></div>)}</div></main>}
