'use client';
import {FormEvent,useEffect,useState} from 'react';
type P={id:string;student_id:string;full_name:string;email:string;class_name:string};
export default function StudentManager(){const [students,setStudents]=useState<P[]>([]);const [form,setForm]=useState({fullName:'',studentId:'',email:'',password:'',className:'',dateOfBirth:'',guardianName:''});const [message,setMessage]=useState('');async function load(){const r=await fetch('/api/admin/students');const d=await r.json();if(r.ok)setStudents(d.students||[])}useEffect(()=>{load()},[]);async function submit(e:FormEvent){e.preventDefault();setMessage('');const r=await fetch('/api/admin/students',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setMessage(d.error||'Could not create account.');return}setMessage(`Pupil account created: ${d.studentId}`);setForm({fullName:'',studentId:'',email:'',password:'',className:'',dateOfBirth:'',guardianName:''});load()}return <section className="mt-12 grid gap-8 lg:grid-cols-[.85fr_1.15fr]"><form onSubmit={submit} className="rounded-[2rem] border border-[var(--line)] bg-[var(--surface)] p-7"><p className="eyebrow text-[var(--gold)]">New pupil</p><h2 className="display mt-3 text-4xl">Create login credentials</h2>{Object.entries(form).map(([key,value])=><input key={key} value={value} onChange={e=>setForm({...form,[key]:e.target.value})} required={['fullName','studentId','email','password','className'].includes(key)} type={key==='email'?'email':key==='password'?'password':key==='dateOfBirth'?'date':'text'} placeholder={key.replace(/([A-Z])/g,' $1')} className="mt-4 w-full border-b border-[var(--line)] bg-transparent py-3 outline-none focus:border-[var(--brand)]"/>)}{message&&<p className="mt-4 text-sm text-[var(--brand)]">{message}</p>}<button className="mt-6 rounded-full bg-[var(--brand)] px-6 py-3 font-bold text-white">Create pupil</button></form><div className="rounded-[2rem] border border-[var(--line)] bg-[var(--surface)] p-7"><p className="eyebrow">Pupil accounts</p><div className="mt-6 divide-y divide-[var(--line)]">{students.length?students.map(p=><div key={p.id} className="py-4"><div className="flex flex-wrap justify-between gap-2"><strong>{p.full_name}</strong><span className="font-mono text-xs text-[var(--brand)]">{p.student_id}</span></div><p className="mt-1 text-sm text-[var(--muted)]">{p.class_name} · {p.email}</p></div>):<p className="py-8 text-[var(--muted)]">No pupil accounts yet.</p>}</div></div></section>}
