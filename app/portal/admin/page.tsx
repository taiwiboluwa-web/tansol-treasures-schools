import { requireRole } from '@/lib/auth/server'; export default async function AdminPage(){const s=await requireRole(['admin']);return <main className="page-shell py-20"><p className="eyebrow text-[var(--gold)]">Administration</p><h1 className="display mt-4 text-6xl md:text-8xl">The school, seen as a system.</h1><p className="mt-6 max-w-2xl leading-7 text-[var(--muted)]">Welcome {s.fullName}. This control centre is the protected home for users, academic records, publication and school operations.</p><div className="mt-12 grid gap-4 md:grid-cols-4">{['Students','Staff','Results','Publishing'].map((x,i)=><div key={x} className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-6"><span className="font-mono text-xs text-[var(--gold)]">0{i+1}</span><h2 className="display mt-12 text-3xl">{x}</h2><p className="mt-3 text-sm text-[var(--muted)]">Management module ready for records and workflows.</p></div>)}</div></main>}
