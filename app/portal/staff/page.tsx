import { requireRole } from '@/lib/auth/server'; export default async function StaffPage(){const s=await requireRole(['staff','admin']);return <main className="page-shell py-20"><p className="eyebrow text-[var(--gold)]">Staff workspace</p><h1 className="display mt-4 text-6xl md:text-8xl">Good work starts with clean records.</h1><div className="mt-12 grid gap-4 md:grid-cols-3"><div className="rounded-3xl bg-[var(--green)] p-7 text-white"><p className="eyebrow text-white/60">Signed in</p><p className="mt-8 text-2xl">{s.fullName}</p></div><div className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7"><p className="eyebrow">Gradebook</p><p className="mt-8 text-[var(--muted)]">Grade entry and editing workspace will appear here.</p></div><div className="rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-7"><p className="eyebrow">Submission</p><p className="mt-8 text-[var(--muted)]">Review and submit completed records for admin publication.</p></div></div></main>}
